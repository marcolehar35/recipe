// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == Import

// == Composant
const Nav = () => (
  <nav>
    Nav
  </nav>
);

Nav.propTypes = {};

// == Export
export default Nav;
