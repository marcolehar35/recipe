// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == Import

// == Composant
const TopBar = () => (
  <div>
    TopBar
  </div>
);

TopBar.propTypes = {};

// == Export
export default TopBar;
